# Frontend foundation

Node tooling is pinned for Phase 1. Production React/MapLibre implementation,
TypeScript configuration and component tests begin only after P08.03 design handoff.

```sh
npm ci
npm run check
```
