# Source code

Canonical application tree. Planned folders are `backend/`, `edge/`, `simulator/`,
`frontend/`, `contracts/`, `database/`, `models/`, `security/`, `observability/`,
`infra/`, `scripts/`, and `tests/`. Create them only when the owning task begins;
do not add placeholder implementations.
