# SonarQube Cloud (sonarcloud.io) project configuration.
# organization/projectKey are guesses matching this GitHub account/repo -
# after creating the project at sonarcloud.io, confirm both against the
# values shown there (Administration > General Settings) and fix if different.
sonar.organization=iukhan82
sonar.projectKey=iukhan82_aiops-project-2

sonar.sources=source-code
sonar.exclusions=**/node_modules/**,**/.venv/**,**/dist/**,**/coverage/**,**/__pycache__/**,**/output/**,source-code/frontend/playwright-report/**,source-code/frontend/test-results/**

sonar.python.version=3.12
